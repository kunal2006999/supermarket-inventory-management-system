
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/supermarket_db
    username: root
    password: kunsql248#
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
